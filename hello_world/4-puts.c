#include <stdio.h>
/**
 * main - This is a description of the main function.
 *
 * Description: it should print a line of text.
 * Return: value should be 0 (indicates success).
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
