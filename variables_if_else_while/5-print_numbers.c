#include <stdio.h>
/**
 * main - prints numbers 0-9 in separate lines
 *
 * Return: should return 0 upon success.
 */
int main(void)
{
	int i = 0;

	for (; i <= 9; i++)
	{
		printf("%d", i);
	}
	printf("\n");
return (0);
}
